# LostArk Project Development Log

Lost Ark Open API를 활용한 캐릭터 분석 및 성장 추천 서비스 개발 기록입니다.

---

## 2026-09-07

### 개발 환경 구축

- VS Code 프로젝트 생성
- Python 가상환경 `.venv` 생성
- `requests`, `python-dotenv` 설치
- `.env`를 이용한 Lost Ark Open API Key 관리
- `.gitignore` 설정
- GitHub Repository 연결

### Lost Ark Open API 연동

- Lost Ark Open API 캐릭터 조회 테스트
- 캐릭터 정보 정상 응답 확인
- API 응답 데이터를 JSON 파일로 저장
- `data/raw` 디렉터리에 원본 데이터 저장
- 원본 데이터는 Git 추적 대상에서 제외

### ArmoryProfile 분석

캐릭터 기본 정보를 파싱하도록 구현.

추출 항목:

- 캐릭터명
- 서버
- 클래스
- 캐릭터 레벨
- 아이템 레벨
- 전투력
- 치명
- 특화
- 신속
- 공격력
- 최대 생명력

### ArmoryEquipment 분석

#### 무기

추출 항목:

- 강화 단계
- 아이템 레벨
- 품질
- 무기 공격력
- 추가 피해

#### 방어구

5개 부위의 정보를 파싱하도록 구현.

- 투구
- 상의
- 하의
- 장갑
- 어깨

추출 항목:

- 강화 단계
- 아이템 레벨
- 품질

추가로 평균 방어구 강화 단계와 평균 품질 계산.

### Skill Gem 분석

아크그리드의 Gem과 혼동되지 않도록 기존 보석 관련 변수명을 `skill_gem`으로 통일.

추출 항목:

- 슬롯
- 보석 종류 (겁화 / 작열)
- 보석 레벨
- 등급
- 적용 스킬
- 보석 효과
- 기본 공격력 증가 효과

추가 계산:

- 스킬 보석 개수
- 평균 스킬 보석 레벨

### ArmoryEngraving 분석

`ArkPassiveEffects`에서 현재 사용 중인 각인 정보 확인.

확인 항목:

- 각인 이름
- 각인 레벨
- 등급
- 어빌리티 스톤 레벨
- 각인 설명

### 다음 작업

- `parse_engravings()` 최종 확인
- `ArkPassive` 데이터 구조 분석
- 아크 패시브 데이터 파싱
- 아크그리드 데이터 분석

==============================================================================================

# DEVLOG

## 2026-09-08

### 오늘의 목표

Lost Ark Open API를 통해 수집한 캐릭터 데이터를 실제 웹페이지에서 시각적으로 확인할 수 있도록
LoaP(LostArk_Project) 웹사이트의 1차 버전을 구현했다.

---

### 1. 캐릭터 이미지 및 아이템 이미지 데이터 추가

웹페이지에서 캐릭터와 장비를 이미지로 표시하기 위해
API에서 제공하는 이미지 URL을 processed JSON에 포함하도록 파서를 수정했다.

추가한 이미지 데이터:

- 캐릭터 전신 이미지
- 무기
- 방어구
- 액세서리
- 팔찌
- 어빌리티 스톤
- 완갑
- 스킬 보석
- 카드
- 아크 패시브
- 아크 그리드 코어 및 젬

기존 분석용 데이터뿐 아니라 프론트엔드에서도 사용할 수 있도록
이미지 URL을 processed 데이터에 유지하도록 변경했다.

---

### 2. FastAPI 웹 서버 구축

FastAPI를 이용해 LoaP 웹사이트의 기본 서버를 구축했다.

추가 패키지:

- FastAPI
- Uvicorn
- Jinja2

웹 요청 흐름:

캐릭터 검색
→ Lost Ark Open API 호출
→ raw 데이터 저장
→ character_parser를 통한 데이터 파싱
→ processed JSON 저장
→ Jinja2 템플릿 렌더링

`collect_character()`를 웹에서도 재사용하도록 구성하여
기존 데이터 수집 파이프라인과 웹사이트를 연결했다.

---

### 3. Python 패키지 구조 정리

FastAPI 실행 과정에서 backend 내부 모듈 import 문제가 발생했다.

backend 폴더에 `__init__.py`를 추가하고 import 구조를 다음과 같이 변경했다.

backend.app
→ backend.collect_character
→ backend.character_parser

이에 따라 캐릭터 수집 스크립트도 다음과 같이 실행할 수 있도록 정리했다.

```bash
python -m backend.collect_character 캐릭터이름

### 4. LoaP 메인 검색 페이지 구현

프로젝트 웹사이트 이름을 다음과 같이 결정했다.

**LoaP = LostArk_Project**

메인 페이지에 캐릭터 검색창을 구현했으며,  
검색한 캐릭터 이름을 이용해 `/character/{character_name}` 페이지로 이동하도록 구성했다.

주요 기능:

- LoaP 로고 표시
- 캐릭터 이름 입력
- 검색 버튼
- 검색한 캐릭터의 상세 페이지로 이동

---

### 5. 캐릭터 정보 페이지 1차 UI 구현

직접 작성한 레이아웃 시안을 기반으로 캐릭터 정보 페이지를 구현했다.

현재 화면 구성:

- LoaP 로고
- 상단 캐릭터 검색창
- 캐릭터 전신 이미지
- 캐릭터 기본 정보
- 공격력
- 전투력
- 치명
- 특화
- 신속
- 아크 패시브
- 아크 그리드
- 무기 및 방어구
- 완갑
- 목걸이
- 귀걸이 2개
- 반지 2개
- 팔찌
- 어빌리티 스톤
- 스킬 보석

전체적으로 **다크 테마**를 사용하고,  
Lost Ark 캐릭터 정보 사이트 형태의 UI를 구성했다.

---

### 6. 장비 및 액세서리 UI 정렬 개선

장비와 액세서리가 좌우로 동일한 비율로 정렬되도록 수정했다.

왼쪽 장비 구성:

1. 투구
2. 어깨
3. 상의
4. 하의
5. 장갑
6. 무기
7. 완갑

오른쪽 장비 구성:

1. 목걸이
2. 귀걸이 1
3. 귀걸이 2
4. 반지 1
5. 반지 2
6. 팔찌
7. 어빌리티 스톤

각 카드의 높이와 아이콘 영역을 일정하게 만들어  
좌우 장비가 깔끔하게 대응하도록 수정했다.

---

### 7. 스킬 보석 UI 개선

스킬 보석을 종류에 따라 구분하여 표시하도록 변경했다.

보석 표시 순서:

1. **겁화**
2. **작열**
3. **광휘**

보석 종류별 용도:

- **겁화**: 피해 증가
- **작열**: 재사용 대기시간 감소
- **광휘**: 광휘 보석

각 보석에는 다음 정보를 표시한다.

- 보석 이미지
- 보석 레벨
- 보석 종류
- 적용 스킬
- 보석 효과

또한 같은 종류의 보석에서는  
**높은 레벨의 보석이 먼저 표시되도록 정렬**했다.

예시:

```text
스킬 보석

겁화 - 피해 증가
├─ Lv.9 겁화
├─ Lv.9 겁화
└─ ...

작열 - 재사용 대기시간 감소
├─ Lv.9 작열
├─ Lv.9 작열
└─ ...

광휘
└─ ...
```

---

### 8. FastAPI와 기존 데이터 수집 파이프라인 연결

웹사이트에서 캐릭터를 검색하면 기존에 구현한 `collect_character()` 함수를 사용하도록 연결했다.

전체 처리 흐름:

```text
캐릭터 이름 검색
        ↓
Lost Ark Open API 호출
        ↓
data/raw 저장
        ↓
character_parser.py
        ↓
데이터 정규화
        ↓
data/processed 저장
        ↓
FastAPI
        ↓
Jinja2 Template
        ↓
캐릭터 정보 웹페이지 출력
```

기존 데이터 수집 및 파싱 코드를 웹에서도 그대로 재사용할 수 있도록 구성했다.

---

### 9. 확인된 이슈

일부 캐릭터의 무기 Tooltip 내부 값이 `null`인 경우  
`parse_weapon()`에서 오류가 발생할 수 있다는 것을 확인했다.

현재는 웹사이트 UI 구현을 우선하기 위해  
해당 예외 처리는 추후 수정하기로 했다.

예외가 발생하는 형태:

```text
AttributeError:
'NoneType' object has no attribute 'get'
```

캐릭터별 API 데이터 구조가 일부 다를 수 있으므로  
향후 여러 캐릭터를 수집하는 단계에서 parser의 예외 처리를 강화할 예정이다.

---

### 다음 개발 예정

- 캐릭터 정보 페이지 UI 세부 개선
- 장비 등급에 따른 색상 표현
- 아이템 품질에 따른 시각적 표현
- 장비 Tooltip 기능
- 아크 패시브 상세 UI
- 아크 그리드 상세 UI
- 카드 정보 UI
- 다양한 캐릭터 데이터에 대한 예외 처리 강화
- 캐릭터 데이터 대량 수집
- Pandas를 이용한 데이터 분석
- 비슷한 아이템 레벨 캐릭터 비교 기능
- 스펙업 추천 기능